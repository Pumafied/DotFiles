# This script creates logging by appending print("")


# Get the line number and make sure that the line doesn't already exist

# this is the thing to use to generate the line
# to_print = "print(\"Called: {}\")".format(method_name)
